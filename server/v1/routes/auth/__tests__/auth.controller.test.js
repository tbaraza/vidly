/* eslint-disable no-undef */
describe('test tests', () => {
  it('ensures the test', () => {
    expect(1).toEqual(1);
  });
});

describe('test tests', () => {
  it('ensures the test', () => {
    expect(1).toEqual(1);
  });
});
